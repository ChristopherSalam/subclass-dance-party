var hasMajorDiagonalConflictAt = function(columnIndexAtFirstRow) {
	// iterate through the rows on the board
		// check to see if that square we are checking is on the board
			// if it is, increment the sum by the current square
		// increase the columnIndex
	// check to see if our sum is greater than 1;
};